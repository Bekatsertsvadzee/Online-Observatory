# Tooling and runtime for the Darkview Observatory Agent development environment.
# Python 3.12 per CLAUDE.md. Create with:
#   python3.12 -m venv agent/.venv
#   agent/.venv/bin/pip install -r agent/requirements-dev.txt
#
# A venv does not survive being moved. If the repository folder is renamed or
# relocated, delete agent/.venv and recreate it.

# Contract code generation: produces agent/contracts/models.py from openapi.yaml.
datamodel-code-generator[http]==0.76.0

# Runtime. The email extra is required — the generated models use EmailStr.
pydantic[email]==2.12.4

# Frame data. The simulator renders a synthetic star field and DV-033 live-stacks
# real frames; both are array work that numpy is the standard tool for.
numpy==2.3.4

# The outbound link. websockets backs the real transport; the agent dials out
# and never binds a port.
websockets==15.0.1

# Test and lint. psutil proves the process holds no listening socket.
psutil==7.1.3
pytest==8.4.2
pytest-cov==7.0.0
ruff==0.14.4
